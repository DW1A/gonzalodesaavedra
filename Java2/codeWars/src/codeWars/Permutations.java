package codeWars;

import java.util.ArrayList;
import java.util.Arrays;
import java.util.List;

class Permutations {
    
    public static List<String> singlePermutations(String s) {
    	List<String> allPerms = new ArrayList<>();
        StringBuilder singlePerm = new StringBuilder();
        
        List<String> singleCharacters = new ArrayList<>(Arrays.asList(s.split("")));
        
        for (int i = 0; i < singleCharacters.size(); i++) {
			
		}
    	
        return null;
    }
}